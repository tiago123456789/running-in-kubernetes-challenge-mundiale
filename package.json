{
  "name": "challenge_mundiale",
  "version": "1.0.0",
  "description": "Challenge mundiale implemented crawler",
  "main": "./src/index.js",
  "scripts": {
    "start": "npm run build && pm2 ./build/index.js",
    "build": "babel ./src --out-dir ./build",
    "start:dev": "nodemon --exec babel-node --presets=env ./src/index.js",
    "playredis": "nodemon --exec babel-node --presets=env ./src/playredis.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tiago R. data costa",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "axios": "^0.19.2",
    "body-parser": "^1.19.0",
    "cheerio": "^1.0.0-rc.3",
    "compression": "^1.7.4",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "redis": "^3.0.2",
    "winston": "^3.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^2.0.3"
  }
}
